seed: 20260430
epochs: 120
learning_rate: 0.005
hidden_dim: 32
num_layers: 2
model_id: brep_assembly_net_v001
target:
  mesh_size_source: mesh_size_labels
  primary_size_heads:
    - face_size
    - edge_size
    - contact_size
  confidence_floor: 0.55
